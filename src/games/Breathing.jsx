import { useEffect, useState } from "react";
export default function Breathing() { const [phase,setPhase]=useState("Breathe in"); const [seconds,setSeconds]=useState(4); useEffect(()=>{const id=setInterval(()=>setSeconds(s=>{if(s<=1){setPhase(p=>p==="Breathe in"?"Hold" : p==="Hold"?"Breathe out":"Breathe in");return phase==="Breathe out"?4:4;} return s-1;}),1000);return()=>clearInterval(id)},[phase]); return <div className="game-content breath-game"><div className="breath-orb"><b>{phase}</b><span>{seconds}</span></div><p>Follow the circle gently. There is no need to force the breath.</p></div>; }
